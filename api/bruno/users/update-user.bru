meta {
  name: update user
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/api/v1/users/{{userId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "firstName": "Jane",
    "lastName": "Doe",
    "email": "jane@example.com"
  }
}
